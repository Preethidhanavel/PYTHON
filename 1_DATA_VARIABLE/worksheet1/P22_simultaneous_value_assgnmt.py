#Assign the same value (100) simultaneously to three variables (a, b, and c), then print each variable.
a = b = c = 100   # Assign the value 100 to all three variables a, b, and c

print("a=", a, "b=", b, "c=", c)   # Print the values of a, b, and c
